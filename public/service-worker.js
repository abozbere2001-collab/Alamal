// A basic service worker
self.addEventListener('fetch', (event) => {
  // We are not adding any caching logic for now to keep it simple.
  // The presence of this file is enough to make the app installable.
});
